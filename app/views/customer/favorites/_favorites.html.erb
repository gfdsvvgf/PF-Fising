<% if list.favorited_by?(current_customer) %>
     <%= link_to customer_todolist_favorites_path(list), method: :delete, remote: true do %>
        ♥<%= list.favorites.count %> 
     <% end %>
    <% else %>
     <%= link_to customer_todolist_favorites_path(list), method: :post, remote: true do %>
        ♡<%= list.favorites.count %> 
     <% end %>
<% end %>