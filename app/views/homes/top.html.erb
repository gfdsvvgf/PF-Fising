<div class="container">
  <div class="row">
    <div class='mx-auto col-11 col-sm-10 col-lg-8 col-xl-6'>
      <h1>ようこそ <i class="fas fa-fish"></i><strong>Fishingへ</strong>  !!</h1>
      <p>ここは釣り具（ルアーやロッドなど）のレビューサイトです</p>
      <p>このサイトを利用するには最初に会員登録が</p>
      <p> 必要になります!</p>
      <div class="btn-wrapper col-10 mx-auto">
        <div class="row">
          <%= link_to 'Log in',new_customer_session_path, class: "btn btn-info btn-sm btn-block mb-3 sign_in" %>
        </div>
        <div class="row">
          <%= link_to 'Sign Up',new_customer_registration_path, class: "btn btn-success btn-sm btn-block sign_up" %>
        </div>
      </div>
      <table class="table">
        <p>投稿一覧</p>
          <thead class="thead-light">
            <tr>
              <th>title</th>
              <th>genre</th>
              <th>Opinion</th>
            </tr>
          </thead>
          <tbody>
            <% @lists.each do |list| %>
            <td><%= list.title %></td>
            <td><%= list.genre.name %></td>
            <td><%= list.body %></td>
            <% end %>
          </tbody>
      </table>
    </div>
  </div>
</div>
